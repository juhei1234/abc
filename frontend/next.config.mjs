/** ABOUTME: Next.js config for SPA frontend rendering analytics and calendar data. */
/** ABOUTME: Keeps configuration minimal for deployable starter behavior. */
const nextConfig = {};

export default nextConfig;
